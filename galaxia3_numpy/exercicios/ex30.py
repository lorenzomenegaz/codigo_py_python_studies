import numpy as np

cotacao = float(input('digite a cotação de uma acao: '))
cotacao2 = float(input('digite a cotação de uma acao: '))
cotacao3 = float(input('digite a cotação de uma acao: '))

array = np.array([cotacao, cotacao2, cotacao3])

print(array)